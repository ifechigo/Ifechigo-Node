extends layout

block layout-content
    .View.UserView
        h1.Banner Howdy, #{userProfile.nickname}
        .Message
            .Title
                h3 Making Me the Best
                h1 Hi Profile
            pre.Details=JSON.stringify(userProfile, null, 2)
        .NavButtons
            a(href="/")
                .NavButton log out

